import { Link } from 'gatsby'

const PlaceholderBlock = ({ classes }) => {
  return (
    <div className={`${classes} bg-white/75 p-6`}>
      Placeholder block
    </div>
  )
}

export default PlaceholderBlock
